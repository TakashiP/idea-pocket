<%= form_for(@idea) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "アイデアを入力してください" %>
  </div>
  <%= f.submit "作成", class: "btn btn-primary" %>
<% end %>